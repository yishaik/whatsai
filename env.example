# --- Preferred: Cloudflare Workers AI (replaces Gemini + OpenAI keys) ---
# Account ID: dash.cloudflare.com → any domain → the hex ID in the URL, or `npx wrangler whoami`
# Token: dash.cloudflare.com/profile/api-tokens → Create Token → Workers AI (Read)
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=

# --- Optional OpenAI-compatible providers (free-tier keys are fine) ---
GROQ_API_KEY=
CEREBRAS_API_KEY=
OPENROUTER_API_KEY=
NVIDIA_API_KEY=

# --- Live voice (Cloudflare Voice Worker: Flux STT + Aura TTS + Llama) ---
# Public workers.dev URL from `npx wrangler deploy` (no trailing slash).
VOICE_AGENT_HOST=http://localhost:8787

# --- Optional leftover Gemini path if Cloudflare is unset ---
GEMINI_API_KEY=

# --- Optional leftover GPT path if Cloudflare is unset ---
OPENAI_API_KEY=

# --- Convex ---
CONVEX_DEPLOYMENT=
VITE_CONVEX_URL=
VITE_CONVEX_SITE_URL= 